# Rust
/target/
**/target/
**/*.rs.bk
*.pdb

# Ad-hoc scratch experiments — compiled binaries and one-off sources never
# belong at the repo root (a `test_size_hint` binary once slipped in).
/test_size_hint
/test_size_hint.rs

# LaTeX build artifacts (theory/papers)
*.aux
*.log
*.out
*.toc
*.synctex.gz

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Rendered HTML artifacts — source docs are Markdown.
# docs/AllEle.html is the exception: an external paper, intentionally tracked.
/CLAUDE.html
/docs/ufl-first-draft.html
